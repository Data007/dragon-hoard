= content_for :page_title do
  %h1 Frequently Asked Questions
  
= content_for :page_actions do
  = raw(button_for_url("edit", edit_admin_faq_path(@faq), :class => "button"))

- content_for :page_content do
  :markdown
    #{@faq.header}
    
    #{ @faq.body}
