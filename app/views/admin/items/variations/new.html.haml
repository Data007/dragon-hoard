- images = @variation.assets
  
- content_for :page_title do
  %h1 New Variation

- if @variation.new_record?
  - content_for :page_actions do
    = raw button_for_url("cancel", [:cancel, :admin, @item, @variation], :class => "button")
  
- content_for :page_content do
  = render :partial => "admin/items/variations/form"
