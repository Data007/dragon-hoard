%h2 Available Inventory
%table
  %tbody
    - @items.each do |item|
      %tr
        %td
          - if item.assets
            - item.assets.by_position[0..3].each do |asset|
              = link_to image_tag(asset.image.url(:thumbnail)), edit_admin_item_path(item.pretty_id)
        %td
          %b= link_to "ID#{item.pretty_id}", edit_admin_item_path(item.pretty_id)
        %td
          Quantity:
          %b #{item.quantity}
        %td
          Price:
          %b $#{item.price}
%h2 Retail Summary
%table
  %thead
    %tr
      %th{style: 'text-align: left; padding-right: 2em;'} Quantity Total
      %th{style: 'text-align: left; padding-right: 2em;'} Retail Price Total
  %tbody
    %tr
      %td= @quantity_total
      %td= number_to_currency @retail_total
